@model IEnumerable<LabDeskApplication.Models.SetupArticleType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Add Article Type", "Create", null, new { @class = "btn btn-primary" }) |
    @Html.ActionLink("Initial Log - Setup", "Index", "Log03InitialStyle", null, new { @class = "btn btn-info" })
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ArticleType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ArticleType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArticleTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArticleTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArticleTypeId })
        </td>
    </tr>
}

</table>
